const Card = () => {
  return (
    <div className="container border px-6 py-6 rounded border-solid border-[#e8eaee] bg-slate-50">
      <h4 className="text-black font-semibold">1. User registration Form</h4>
      <p className="mt-2">
        Create a user registration form with fields for username, email, and
        password. Validate inputs and display appropriate error messages
      </p>
    </div>
  );
};

export default Card;
